# Copy this file to frontend/.env and fill in real values.

# Base URL the frontend's API client (src/lib/api.js) sends every request
# to. Must match a backend host that is itself listed in the backend's
# CORS_ORIGINS.
REACT_APP_BACKEND_URL=http://localhost:8000
